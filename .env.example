# Copy to .env.local and fill in. See docs/ENV_SETUP.md and docs/SUPABASE_CONNECTION.md.

# === Prisma (required) ===
# Supabase Connect → Transaction (port 6543). Must include &pgbouncer=true.
DATABASE_URL="postgres://postgres.YOUR_PROJECT_REF:YOUR_PASSWORD@aws-0-YOUR_REGION.pooler.supabase.com:6543/postgres?sslmode=require&pgbouncer=true"

# Optional: same URL; app uses this in production (Vercel) when set.
# POSTGRES_PRISMA_URL="postgres://..."

# === Ingest API (NetSuite / ingest-batch) ===
# API_KEY=
# API_SECRET=

# === ShipEngine (create-label, get-services) ===
# SHIPENGINE_API_KEY=

# === ShipStation (create-order) ===
# SHIPSTATION_API_KEY=
# SHIPSTATION_API_SECRET=
